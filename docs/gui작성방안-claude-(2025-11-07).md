# CSA GUI 구현 방안

**작성일**: 2025-11-07
**대상**: CSA (Code Static Analyzer) GUI 프로토타입 개발
**요구사항**: 하이브리드 앱, 개발자 + PM 혼합 사용자, 분석 모니터링 + 그래프 시각화

---

## 목차

1. [기술 스택 추천](#기술-스택-추천)
2. [아키텍처 설계](#아키텍처-설계)
3. [상세 기술 결정](#상세-기술-결정)
4. [개발 로드맵](#개발-로드맵)
5. [구현 전략](#구현-전략)

---

## 기술 스택 추천

### 🏆 권장 스택: Tauri + Vue.js + D3.js + FastAPI

| 레이어 | 기술 | 선택 이유 |
|--------|------|---------|
| **데스크톱 프레임워크** | [Tauri](https://tauri.app/) | 가볍고 빠르며, Rust 기반으로 보안 강화, 메모리 효율적 |
| **프론트엔드 프레임워크** | Vue.js 3 | 점진적 학습곡선, 빠른 개발, 작은 번들 크기 |
| **UI 컴포넌트** | Ant Design Vue | 전문적인 UI, 풍부한 컴포넌트, 기업용 디자인 |
| **그래프 시각화** | D3.js + Cytoscape.js | 강력한 커스터마이징, Neo4j 데이터 친화적 |
| **다이어그램 렌더링** | Mermaid.js | 이미 사용 중, 시퀀스/CRUD 지원 |
| **백엔드 API** | FastAPI | Python 통합, 빠른 성능, WebSocket 지원 |
| **상태 관리** | Pinia | Vue 3 공식 상태 관리, 간단하고 강력함 |
| **실시간 통신** | WebSocket | 분석 진행 상황 실시간 스트리밍 |
| **차트/그래프** | ECharts + Vis.js | 복잡한 데이터 시각화, 대규모 노드 렌더링 |

### 🥈 대안 스택: Electron + React

| 레이어 | 기술 | 장점/단점 |
|--------|------|---------|
| **데스크톱 프레임워크** | Electron | ✅ 성숙한 생태계, 많은 라이브러리<br/>❌ 메모리 사용량 높음 (최소 ~200MB) |
| **프론트엔드** | React 18 | ✅ 대규모 커뮤니티, 풍부한 라이브러리<br/>❌ 번들 크기 커짐 |
| **UI 컴포넌트** | Material-UI | ✅ Google 디자인 언어<br/>❌ Ant Design보다 무거움 |
| **나머지** | 위와 동일 | - |

---

## 아키텍처 설계

### 📐 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                 CSA GUI 애플리케이션                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │           Tauri 데스크톱 프레임워크                   │  │
│  │          (네이티브 윈도우, 메뉴, 파일 접근)          │  │
│  └─────────────────────────────────────────────────────┘  │
│                         │                                  │
│  ┌──────────────────────┴──────────────────────────────┐  │
│  │                                                      │  │
│  │   ┌──────────────────────────────────────────────┐  │  │
│  │   │        Vue.js 3 (Frontend SPA)               │  │  │
│  │   │  ┌─────────────────────────────────────┐     │  │  │
│  │   │  │  Pages (Dashboard, Analysis, etc)   │     │  │  │
│  │   │  ├─────────────────────────────────────┤     │  │  │
│  │   │  │  Components (Sidebar, Tabs, etc)    │     │  │  │
│  │   │  ├─────────────────────────────────────┤     │  │  │
│  │   │  │  Stores (Pinia - State Management)  │     │  │  │
│  │   │  ├─────────────────────────────────────┤     │  │  │
│  │   │  │  Utils (API, WebSocket, Helpers)    │     │  │  │
│  │   │  └─────────────────────────────────────┘     │  │  │
│  │   └──────────────────────────────────────────────┘  │  │
│  │                    │                                │  │
│  └────────────────────┼────────────────────────────────┘  │
│                       │ HTTP/WebSocket                    │
│                       ↓                                   │
└─────────────────────────────────────────────────────────────┘
                        │
        ┌───────────────┴──────────────────┐
        │                                  │
        ↓                                  ↓
┌──────────────────────┐        ┌──────────────────────┐
│   FastAPI 백엔드      │        │  기존 Python CLI     │
│  (Python + Uvicorn)  │        │  (csa.cli.main)      │
│                      │        │                      │
│ ┌──────────────────┐ │        │ ┌──────────────────┐ │
│ │ API Routes       │ │        │ │ 분석 엔진         │ │
│ ├──────────────────┤ │        │ ├──────────────────┤ │
│ │ • Projects       │ │        │ │ • Java Parser    │ │
│ │ • Analysis       │ │        │ │ • DB Parser      │ │
│ │ • Results        │ │        │ │ • Graph Builder  │ │
│ │ • Config         │ │        │ │ • AI Enrich      │ │
│ │ • WebSocket      │ │        │ └──────────────────┘ │
│ └──────────────────┘ │        └──────────────────────┘
└──────────────────────┘
        │
        ↓
┌──────────────────────────────────┐
│     Neo4j Graph Database         │
└──────────────────────────────────┘
```

### 📂 프로젝트 구조

```
csa-gui/
├── src/
│   ├── components/               # 재사용 가능한 Vue 컴포넌트
│   │   ├── common/               # 공통 컴포넌트
│   │   │   ├── Sidebar.vue
│   │   │   ├── Header.vue
│   │   │   ├── Tabs.vue
│   │   │   └── StatusBar.vue
│   │   ├── analysis/            # 분석 관련 컴포넌트
│   │   │   ├── AnalysisMonitor.vue
│   │   │   ├── ProgressBar.vue
│   │   │   └── LogViewer.vue
│   │   ├── graph/               # 그래프 시각화
│   │   │   ├── GraphViewer.vue
│   │   │   ├── GraphLegend.vue
│   │   │   └── NodeDetail.vue
│   │   ├── diagram/             # 다이어그램 뷰어
│   │   │   ├── DiagramViewer.vue
│   │   │   └── DiagramToolbar.vue
│   │   └── config/              # 설정 컴포넌트
│   │       ├── ConfigEditor.vue
│   │       └── ConfigValidation.vue
│   │
│   ├── pages/                    # 페이지 컴포넌트
│   │   ├── Dashboard.vue
│   │   ├── Analysis.vue
│   │   ├── GraphViewer.vue
│   │   ├── DiagramViewer.vue
│   │   ├── ImpactAnalysis.vue
│   │   ├── ClassSpec.vue
│   │   ├── Configuration.vue
│   │   ├── Settings.vue
│   │   └── Help.vue
│   │
│   ├── stores/                   # Pinia 상태 관리
│   │   ├── projectStore.js
│   │   ├── analysisStore.js
│   │   ├── graphStore.js
│   │   ├── configStore.js
│   │   └── uiStore.js
│   │
│   ├── api/                      # API 통신
│   │   ├── client.js            # Axios 인스턴스
│   │   ├── projectApi.js
│   │   ├── analysisApi.js
│   │   ├── graphApi.js
│   │   └── configApi.js
│   │
│   ├── utils/                    # 유틸 함수
│   │   ├── websocket.js         # WebSocket 관리
│   │   ├── graph-utils.js       # 그래프 처리
│   │   ├── format-utils.js      # 포맷팅
│   │   └── storage.js           # 로컬 스토리지
│   │
│   ├── styles/
│   │   ├── main.css             # 글로벌 스타일
│   │   ├── themes.css           # 테마
│   │   └── responsive.css       # 반응형
│   │
│   ├── App.vue
│   └── main.js
│
├── src-tauri/                    # Tauri 백엔드 (Rust)
│   ├── src/
│   │   ├── main.rs              # Tauri 초기화
│   │   └── commands/            # Tauri 명령어
│   │       ├── analysis.rs
│   │       └── file.rs
│   └── tauri.conf.json
│
├── backend/                      # FastAPI 백엔드
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # FastAPI 앱 초기화
│   │   ├── routes/
│   │   │   ├── projects.py
│   │   │   ├── analysis.py
│   │   │   ├── graph.py
│   │   │   ├── results.py
│   │   │   └── config.py
│   │   ├── schemas/
│   │   │   ├── project.py
│   │   │   ├── analysis.py
│   │   │   └── config.py
│   │   ├── services/
│   │   │   ├── analysis_service.py  # CSA 통합
│   │   │   ├── graph_service.py     # Neo4j 쿼리
│   │   │   └── ws_manager.py        # WebSocket 관리
│   │   └── config.py
│   │
│   ├── requirements.txt
│   └── run.py
│
├── public/                       # 정적 리소스
│   └── icons/
│
├── package.json
├── vite.config.js              # Vite 설정
├── tauri.conf.json
└── README.md
```

---

## 상세 기술 결정

### 1. 데스크톱 프레임워크: Tauri vs Electron

#### Tauri ✅ (권장)

**장점:**
- 📦 **가볍다**: 최소 번들 크기 ~10MB (Electron은 ~150MB)
- 🚀 **성능**: Rust 기반 백엔드, 네이티브 성능
- 🔒 **보안**: Rust의 메모리 안전성, 샌드박스 기본 제공
- 💾 **메모리**: ~50MB (Electron은 ~200MB)
- 🌐 **크로스 플랫폼**: Windows, macOS, Linux 지원
- 🎨 **UI**: 시스템 네이티브 UI 활용 가능

**단점:**
- 🆕 생태계가 비교적 새로움 (하지만 빠르게 성장 중)
- 📚 한국 자료 부족 (영문 문서 충분함)

#### Electron (대안)

**장점:**
- 🏆 성숙한 생태계
- 📚 풍부한 한국어 자료
- 🔌 수많은 라이브러리

**단점:**
- 💻 메모리 사용량 높음 (개발 환경에서는 ~400MB)
- 📦 번들 크기 큼
- 🐌 느린 스타트업

**결론**: **Tauri 권장** - CSA 사용자는 대규모 프로젝트 분석으로 인해 이미 높은 메모리 사용. 가벼운 GUI가 필수.

---

### 2. 프론트엔드 프레임워크: Vue vs React

#### Vue.js 3 ✅ (권장)

**장점:**
- 📈 **점진적 학습**: 간단한 문법, 빠른 학습곡선
- 🎯 **효율적**: 더 적은 코드로 더 많은 기능
- 📦 **번들**: React보다 50% 작음 (~35KB vs ~42KB)
- 🔧 **공식 도구**: Pinia (상태관리), Vue Router, Vite
- 🎨 **유연함**: 작은 프로젝트부터 대규모 프로젝트까지

**단점:**
- 👥 커뮤니티: React보다 작음 (하지만 한국에서는 증가 중)

#### React (대안)

**장점:**
- 👥 **큰 커뮤니티**: 라이브러리 풍부
- 💼 **업계 표준**: 많은 기업에서 사용
- 🔌 **라이브러리**: Next.js, Redux 등 많음

**단점:**
- 📚 **학습곡선**: 더 복잡한 개념 (HOC, Hooks 등)
- 📦 **번들**: 더 큼
- 🔧 **설정**: 복잡한 설정 필요

**결론**: **Vue.js 3 권장** - 빠른 개발, 작은 번들, Tauri와의 호환성

---

### 3. 그래프 시각화: D3.js vs Cytoscape.js vs Vis.js

| 라이브러리 | 용도 | 장점 | 단점 |
|----------|------|------|------|
| **D3.js** | 일반 데이터 시각화 | 강력한 커스터마이징, 아름다운 시각화 | 가파른 학습곡선, 많은 코드 |
| **Cytoscape.js** | 그래프/네트워크 시각화 | Neo4j 친화적, 레이아웃 알고리즘 | 커뮤니티 작음 |
| **Vis.js** | 대규모 네트워크 시각화 | 수천 개 노드 렌더링, 빠름 | 커스터마이징 제한적 |
| **Force-Graph** | 3D/2D 그래프 | 아름다운 시각화, 상호작용 | 성능 (큰 그래프) |

**권장 조합:**
- **클래스 관계도 (중규모)**: Cytoscape.js + D3.js
- **호출 체인 (대규모)**: Vis.js
- **대시보드 차트**: ECharts

---

### 4. 백엔드: FastAPI + WebSocket

#### FastAPI 아키텍처

```python
# backend/app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from contextlib import asynccontextmanager

app = FastAPI(title="CSA Backend")

# CORS 설정
app.add_middleware(
    CORSMiddleware,
    allow_origins=["tauri://localhost"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# WebSocket 분석 모니터링
@app.websocket("/ws/analysis/{project_id}")
async def websocket_analysis(websocket: WebSocket, project_id: str):
    """
    분석 진행 상황 실시간 스트리밍

    메시지 형식:
    {
        "type": "progress|log|error|complete",
        "data": {...}
    }
    """
    await websocket.accept()
    try:
        while True:
            # 분석 엔진에서 진행 상황 가져오기
            # 클라이언트에 전송
            pass
    finally:
        await websocket.close()

# REST API
@app.post("/api/projects/analyze")
async def start_analysis(request: AnalysisRequest):
    """분석 시작"""
    pass

@app.get("/api/results/{project_id}/graph")
async def get_graph(project_id: str):
    """Neo4j 그래프 데이터 조회"""
    pass
```

#### 주요 API 엔드포인트

```
# 프로젝트 관리
POST   /api/projects                 # 프로젝트 생성
GET    /api/projects                 # 프로젝트 목록
GET    /api/projects/{id}            # 프로젝트 상세
PUT    /api/projects/{id}            # 프로젝트 수정
DELETE /api/projects/{id}            # 프로젝트 삭제

# 분석 실행
POST   /api/analysis/start           # 분석 시작
GET    /api/analysis/{id}/status     # 분석 상태
POST   /api/analysis/{id}/cancel     # 분석 취소
WS     /ws/analysis/{id}             # 분석 진행 스트리밍

# 결과 조회
GET    /api/results/{id}/graph       # Neo4j 그래프 데이터
GET    /api/results/{id}/classes     # 클래스 정보
GET    /api/results/{id}/diagrams    # 다이어그램 목록
GET    /api/results/{id}/impact      # 영향도 분석

# 설정 관리
GET    /api/config                   # 전체 설정
PUT    /api/config                   # 설정 수정
GET    /api/config/schema            # 설정 스키마
POST   /api/config/validate          # 설정 검증
```

---

### 5. 상태 관리: Pinia

```javascript
// src/stores/analysisStore.js
import { defineStore } from 'pinia'

export const useAnalysisStore = defineStore('analysis', {
  state: () => ({
    currentAnalysis: null,
    analyses: [],
    isRunning: false,
    progress: 0,
    logs: [],
    error: null,
  }),

  getters: {
    statusPercentage: (state) => state.progress,
    hasError: (state) => state.error !== null,
  },

  actions: {
    async startAnalysis(projectId, options) {
      this.isRunning = true
      this.progress = 0
      this.logs = []

      // WebSocket 연결
      const ws = new WebSocket(`ws://localhost:8000/ws/analysis/${projectId}`)
      ws.onmessage = (event) => {
        const message = JSON.parse(event.data)
        this.handleMessage(message)
      }
    },

    handleMessage(message) {
      switch (message.type) {
        case 'progress':
          this.progress = message.data.percentage
          break
        case 'log':
          this.logs.push(message.data)
          break
        case 'complete':
          this.isRunning = false
          this.progress = 100
          break
        case 'error':
          this.error = message.data
          this.isRunning = false
          break
      }
    },
  },
})
```

---

### 6. 실시간 통신: WebSocket

#### WebSocket 메시지 프로토콜

```json
// 분석 진행 상황
{
  "type": "progress",
  "data": {
    "percentage": 45,
    "current": 3000,
    "total": 6686,
    "files_per_sec": 1.5,
    "eta_minutes": 25,
    "memory_mb": 1321
  }
}

// 로그 메시지
{
  "type": "log",
  "level": "INFO|WARNING|ERROR",
  "timestamp": "2025-11-07T10:56:27.370",
  "message": "파싱 진행중 [75/6686] (1%)"
}

// 분석 완료
{
  "type": "complete",
  "data": {
    "project_id": "car-center-devlab",
    "duration_seconds": 2040,
    "statistics": {
      "classes": 5432,
      "methods": 23456,
      "beans": 234
    }
  }
}

// 에러
{
  "type": "error",
  "message": "파일 파싱 실패",
  "details": "..."
}
```

---

## 개발 로드맵

### Phase 1: MVP (4주)

**목표**: 기본 기능 동작

- [ ] **기초 인프라**
  - Tauri 프로젝트 설정
  - FastAPI 백엔드 구성
  - Vue.js 기본 레이아웃

- [ ] **분석 실행**
  - 분석 시작 UI
  - WebSocket 진행 상황 스트리밍
  - 로그 뷰어

- [ ] **결과 조회**
  - 클래스 정보 테이블
  - 기본 메트릭 대시보드

**산출물**: 작동하는 MVP

---

### Phase 2: 핵심 기능 (4주)

**목표**: 사용자가 가치를 느끼는 수준

- [ ] **그래프 시각화**
  - Neo4j 데이터 조회
  - 인터랙티브 그래프 렌더링 (Cytoscape.js)
  - 필터링 및 검색

- [ ] **다이어그램 뷰어**
  - Mermaid 다이어그램 렌더링
  - PlantUML 지원
  - 확대/축소, 이동

- [ ] **설정 관리**
  - .env 편집기
  - 설정 검증
  - 저장 및 로드

**산출물**: 완성도 높은 기능들

---

### Phase 3: 고도화 (4주)

**목표**: 프로덕션 준비

- [ ] **영향도 분석 UI**
  - 영향도 분석 결과 시각화
  - 테이블/메서드별 분석

- [ ] **클래스 명세서**
  - 클래스 상세 정보
  - 메서드 목록 및 호출 관계
  - 내보내기 기능

- [ ] **성능 최적화**
  - 대규모 그래프 렌더링 (Vis.js)
  - 가상 스크롤링
  - 메모리 최적화

- [ ] **사용자 경험**
  - 어두운 테마 지원
  - 다국어 지원 (한국어, 영어)
  - 키보드 단축키

**산출물**: 프로덕션 준비 완료

---

## 구현 전략

### 1단계: 개발 환경 설정

```bash
# Node.js 환경
node -v  # v18.x 이상
npm -v   # v9.x 이상

# Tauri CLI 설치
npm install -g @tauri-apps/cli

# 프로젝트 생성
npm create tauri-app@latest

# 프로젝트 구조
cd csa-gui
npm install
npm run tauri dev
```

### 2단계: FastAPI 백엔드 설정

```bash
# Python 가상 환경
python -m venv backend-venv
source backend-venv/bin/activate  # Linux/Mac
# backend-venv\Scripts\activate  # Windows

# 의존성 설치
pip install fastapi uvicorn python-socketio python-dotenv
pip install -r requirements.txt

# 개발 서버 실행
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

### 3단계: 통합

```javascript
// Tauri 설정에서 FastAPI 포트 허용
{
  "build": {
    "devPath": "http://127.0.0.1:5173",
    "beforeDevCommand": "npm run dev",
    "beforeBuildCommand": "npm run build",
    "devUrl": "http://127.0.0.1:5173"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "CSA GUI",
        "url": "index.html",
        "width": 1400,
        "height": 900,
        "minWidth": 1024,
        "minHeight": 600
      }
    ]
  }
}
```

---

## 기술 선택 근거 요약

| 선택 | 근거 |
|------|------|
| **Tauri** | CSA 사용자는 이미 고사양을 요구하는 분석 실행 중. GUI는 최대한 가볍게. |
| **Vue.js** | 빠른 개발, 작은 번들, Tauri와 호환성 우수. |
| **FastAPI** | Python 통합, 빠른 성능, WebSocket 기본 지원. |
| **Cytoscape.js + Vis.js** | Neo4j 친화적, 그래프 시각화에 최적화. |
| **Pinia** | Vue 3 공식 상태관리, 간단하고 강력함. |
| **WebSocket** | 실시간 분석 진행 상황 스트리밍, 사용자 경험 향상. |

---

## 주의사항

### 보안

1. **Tauri 샌드박스**: 기본적으로 활성화, 필요 시만 권한 허용
2. **CORS**: `tauri://localhost` 만 허용
3. **환경 변수**: `.env` 파일 암호화 저장
4. **파일 접근**: Tauri의 파일 시스템 API 사용

### 성능

1. **가상 스크롤링**: 수천 개 이상의 노드는 Vis.js 사용
2. **메모리 관리**: WebSocket 메시지 버퍼 관리
3. **이미지 최적화**: 스크린샷 등은 압축 저장

### 호환성

1. **Python 버전**: 3.9 이상
2. **운영체제**: Windows 10+, macOS 10.13+, Ubuntu 18.04+

---

## 참고 자료

### Tauri
- 공식 문서: https://tauri.app/
- GitHub: https://github.com/tauri-apps/tauri

### Vue.js 3
- 공식 문서: https://vuejs.org/
- Pinia: https://pinia.vuejs.org/

### FastAPI
- 공식 문서: https://fastapi.tiangolo.com/
- WebSocket: https://fastapi.tiangolo.com/advanced/websockets/

### 그래프 시각화
- Cytoscape.js: https://js.cytoscape.org/
- D3.js: https://d3js.org/
- Vis.js: https://visjs.org/

---

## 결론

이 기술 스택은 **가볍고, 빠르고, 확장 가능한** CSA GUI를 제공합니다.

- 🚀 **빠른 개발**: Vue.js + Vite로 빠른 개발 사이클
- 💾 **가벼움**: Tauri로 메모리 효율적인 애플리케이션
- 🔒 **안전함**: Rust 기반 보안
- 📈 **확장성**: 모듈화된 아키텍처로 기능 추가 용이

다음 단계: `gui-design/` 폴더에서 상세한 화면 설계를 진행하세요.
